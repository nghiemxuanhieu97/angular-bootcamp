Tuyệt! Chúng ta chuyển sang **Phase 2 – Questions**.

---

# 📚 Level 0 – JavaScript Foundation

# Lesson 13 – Array Methods

# 📝 Questions

## Câu 1

Array Methods được sinh ra để giải quyết vấn đề gì?

---

## Câu 2

`forEach()` dùng để làm gì?

Hãy giải thích bằng ngôn ngữ của bạn.

---

## Câu 3

`map()` được sinh ra để giải quyết vấn đề gì?

Nó trả về kết quả như thế nào?

---

## Câu 4

Cho đoạn code:

```javascript
const names = employees.map(employee => employee.name);
```

Theo bạn:

* Callback là gì?
* `names` sẽ chứa gì?

---

## Câu 5

`filter()` dùng để làm gì?

Nó trả về gì?

---

## Câu 6

Cho đoạn code:

```javascript
const activeEmployees = employees.filter(
    employee => employee.active
);
```

Nếu:

```javascript
employee.active
```

trả về:

* `true`
* `false`

thì điều gì sẽ xảy ra với từng employee?

---

## Câu 7

`find()` và `filter()` khác nhau ở điểm nào?

---

## Câu 8

Cho đoạn code:

```javascript
const total = numbers.reduce(
    (sum, number) => sum + number,
    0
);
```

Theo bạn:

* `sum` là gì?
* `number` là gì?
* `0` là gì?

---

## Câu 9

Trong Angular, vì sao `map()`, `filter()`, `find()` xuất hiện rất nhiều?

Theo cách hiểu hiện tại của bạn.

📌 Chưa cần nói về Change Detection, Signals hay RxJS.

---

## Câu 10

Hãy phân biệt mục đích sử dụng của:

* `forEach()`
* `map()`
* `filter()`
* `find()`
* `reduce()`

Theo cách hiểu của riêng bạn.

---

Mời bạn trả lời 10 câu. Sau khi bạn trả lời xong, mình sẽ chuyển sang **Review & Feedback** theo đúng workflow Bootcamp.
